name: Daily Price Update

on:
  schedule:
    - cron: "0 8 * * 1-5"   # Mon-Fri 8:00 UTC = 4:00 PM Taiwan
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Update dashboard prices
        run: python ci_update.py

      - name: Commit updated dashboard.html
        run: |
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add dashboard.html
          git diff --staged --quiet || git commit -m "chore: daily price update $(date -u +\'%Y-%m-%d\')"
          git push
